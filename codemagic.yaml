workflows:
  ios-adhoc:
    name: iOS Ad-Hoc (direct install)
    instance_type: mac_mini_m2
    max_build_duration: 30
    environment:
      flutter: 3.41.1
      xcode: latest
      cocoapods: default
      vars:
        AUDIO_SESSION_MICROPHONE: "0"
      groups:
        - app-store-credentials
        - app-env
    integrations:
      app_store_connect: Lauschi
    cache:
      cache_paths:
        - $HOME/.pub-cache
        - $HOME/.cocoapods/repos
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
    scripts:
      - name: Register test devices & fetch ad-hoc signing
        script: |
          # Register the iPad if not already known.
          app-store-connect register-device \
            --name "iPad Christopher" \
            --platform IOS \
            --udid "00008101-000A59413651003A" \
            || true
          app-store-connect fetch-signing-files "app.lauschi.lauschi" \
            --type IOS_APP_ADHOC \
            --create
          keychain initialize
          keychain add-certificates
          xcode-project use-profiles

      - name: Write .env file
        script: |
          cat <<EOF > $CM_BUILD_DIR/.env
          SPOTIFY_CLIENT_ID=$SPOTIFY_CLIENT_ID
          SENTRY_DSN=$SENTRY_DSN
          SENTRY_ENVIRONMENT=$SENTRY_ENVIRONMENT
          EOF

      - name: Install dependencies
        script: flutter pub get

      - name: Run code generation
        script: dart run build_runner build --delete-conflicting-outputs

      - name: Build iOS ad-hoc
        script: |
          flutter build ipa --release \
            --build-number=$PROJECT_BUILD_NUMBER \
            --dart-define-from-file=$CM_BUILD_DIR/.env \
            --export-options-plist=/Users/builder/export_options.plist

    artifacts:
      - build/ios/ipa/*.ipa

  ios-release:
    name: iOS Release
    instance_type: mac_mini_m2
    max_build_duration: 30
    environment:
      flutter: 3.41.1
      xcode: latest
      cocoapods: default
      vars:
        AUDIO_SESSION_MICROPHONE: "0"
      groups:
        - app-store-credentials
        - app-env
    integrations:
      app_store_connect: Lauschi
    cache:
      cache_paths:
        - $HOME/.pub-cache
        - $HOME/.cocoapods/repos
    triggering:
      events:
        - push
        - tag
      branch_patterns:
        - pattern: main
          include: true
      tag_patterns:
        - pattern: 'v*'
          include: true
    scripts:
      - name: Set up code signing
        script: |
          app-store-connect fetch-signing-files "app.lauschi.lauschi" \
            --type IOS_APP_STORE \
            --create
          keychain initialize
          keychain add-certificates
          xcode-project use-profiles

      - name: Write .env file
        script: |
          cat <<EOF > $CM_BUILD_DIR/.env
          SPOTIFY_CLIENT_ID=$SPOTIFY_CLIENT_ID
          SENTRY_DSN=$SENTRY_DSN
          SENTRY_ENVIRONMENT=$SENTRY_ENVIRONMENT
          EOF

      - name: Install dependencies
        script: flutter pub get

      - name: Run code generation
        script: dart run build_runner build --delete-conflicting-outputs

      - name: Build iOS
        script: |
          flutter build ipa --release \
            --build-number=$PROJECT_BUILD_NUMBER \
            --dart-define-from-file=$CM_BUILD_DIR/.env \
            --export-options-plist=/Users/builder/export_options.plist

    artifacts:
      - build/ios/ipa/*.ipa

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        expire_build_submitted_for_review: true

  android-release:
    name: Android Release
    instance_type: linux_x2
    max_build_duration: 30
    environment:
      flutter: 3.41.1
      java: 17
      groups:
        - app-env
        - firebase-distribution-credentials
      android_signing:
        - keystore_reference: release_keystore
    cache:
      cache_paths:
        - $HOME/.pub-cache
        - $HOME/.gradle/caches
    triggering:
      events:
        - push
        - tag
      branch_patterns:
        - pattern: main
          include: true
      tag_patterns:
        - pattern: 'v*'
          include: true
    scripts:
      - name: Write .env file
        script: |
          cat <<EOF > $CM_BUILD_DIR/.env
          SPOTIFY_CLIENT_ID=$SPOTIFY_CLIENT_ID
          SENTRY_DSN=$SENTRY_DSN
          SENTRY_ENVIRONMENT=$SENTRY_ENVIRONMENT
          EOF

      - name: Install dependencies
        script: flutter pub get

      - name: Run code generation
        script: dart run build_runner build --delete-conflicting-outputs

      - name: Build Android APK
        script: |
          flutter build apk --release \
            --build-number=$PROJECT_BUILD_NUMBER \
            --dart-define-from-file=$CM_BUILD_DIR/.env

    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk

    publishing:
      firebase:
        firebase_service_account: $FIREBASE_DISTRIBUTION_SERVICE_ACCOUNT_JSON
        android:
          app_id: "1:596360821127:android:98e282cadc806e4c0dbb22"
          groups:
            - testers
